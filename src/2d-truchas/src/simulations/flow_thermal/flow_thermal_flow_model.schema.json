{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://truchas.org/schema/flow_thermal_flow_model_2d.schema.json",
  "title": "Truchas 2D coupled flow model",
  "description": "Flow-model settings used by the coupled flow-thermal simulation. The inertial switch belongs to solver.flow in this simulation; it is deliberately not a flow-model property.",
  "type": "object",
  "properties": {
    "inviscid": {
      "type": "boolean",
      "default": false,
      "description": "Use an inviscid flow model. The viscosity material property is not required in this mode."
    },
    "body-acceleration": {
      "type": "array",
      "prefixItems": [
        { "type": "number" },
        { "type": "number" }
      ],
      "items": false,
      "default": [0.0, 0.0],
      "description": "Constant body acceleration vector [x, y]."
    },
    "bc": {
      "$ref": "../../physics/flow/flow_bc.schema.json",
      "description": "Named flow boundary conditions."
    }
  },
  "required": ["bc"],
  "additionalProperties": false
}
