{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://truchas.org/schema/vof_sim_control_2d.schema.json",
  "title": "Truchas 2D VOF simulation controls",
  "description": "Fixed time-step control and output schedule for the two-dimensional volume-of-fluid simulation.",
  "type": "object",
  "properties": {
    "time-step": {
      "type": "number",
      "exclusiveMinimum": 0.0,
      "description": "Fixed VOF time-step size. It must be strictly positive."
    },
    "initial-time": {
      "type": "number",
      "default": 0.0,
      "description": "Simulation time at which the initial volume fractions are established. The default is zero."
    },
    "final-time": {
      "type": "number",
      "description": "Optional final simulation time. It must be greater than initial-time and no earlier than the last output schedule time; if omitted, it equals the last output schedule time."
    },
    "output-times": {
      "$ref": "#/$defs/output-times",
      "description": "Output schedule. At least one expanded output time must be later than initial-time."
    }
  },
  "required": ["time-step", "output-times"],
  "additionalProperties": false,
  "$defs": {
    "output-times": {
      "title": "VOF output schedule",
      "type": "object",
      "properties": {
        "times": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "number" },
          "description": "Strictly increasing schedule times. At least one expanded time must be later than initial-time."
        },
        "subintervals": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 1
          },
          "description": "Optional number of equally spaced output subintervals between consecutive entries in times. Supply one fewer value than times; if omitted, each interval has one subinterval."
        }
      },
      "required": ["times"],
      "additionalProperties": false
    }
  }
}
