! -*- Mode: f90 -*-

  901 format (a, i1, a)
  902 format (a, i2, a)
  903 format (a, i3, a)
  904 format (a, i4, a)
  905 format (a, i5, a)
  906 format (a, i6, a)
  907 format (a, i7, a)
  908 format (a, i8, a)
  909 format (a, i9, a)
  920 format (a, 1pg9.2, a)
  930 format (a, 1pg10.3, a)
  940 format (a, 1pg11.4, a)
  950 format (a, 1pg12.5, a)
  960 format (a, 1pg13.6, a)
  970 format (a, 1pg14.7, a)
