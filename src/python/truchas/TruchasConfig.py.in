# This file is processed by CMake at configure-time.

import os

class TruchasConfig:
    mpiexec = "@MPIEXEC@"

    exe_dir = "@TruchasExe_BINARY_DIR@"
    truchas_executable = os.path.join(exe_dir, "@Truchas_EXECUTABLE@")
    build_dir = "@Truchas_BINARY_DIR@"
    source_dir = "@Truchas_SOURCE_DIR@"
    write_restart_executable = os.path.join(build_dir, "src/python/bin/write-restart.py")
    test_build_dir = os.path.join(build_dir, "test") #"@TruchasTest_BINARY_DIR@"
    test_source_dir = os.path.join(source_dir, "test") #"@TruchasTest_SOURCE_DIR@"
